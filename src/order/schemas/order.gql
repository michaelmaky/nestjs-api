scalar Date

type Order {
  _id: ID!
  orderNo: String!
  date: Date
  # consignee: OrderContact
  contactAddress: String!
  # charge: OrderCharge!
  remarks: String
  # generated by mongoose {timestamp: true}
  createdAt: Date
  updatedAt: Date
}

type Query {
  getOrder(id: ID!): Order
}
